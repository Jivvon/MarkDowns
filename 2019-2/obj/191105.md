# 191105 객체

## C++ Libraries

**STL**

- container
- Algorithms
- Iterators

**Template**
functions

```c++
template <typename T>
inline T const& Max(T const& a, T const& b)
{
    return a < b ? b : a;>
}
```

### vector

```c++
vector<int> ::iterator ptr = ar.begin();

for(v = vec.begin(); v != vec.end(); v=v.next()){
    cout << *v << endl;
}
```
