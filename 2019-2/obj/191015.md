# 191015 객체

C++에서 구조체 선언 마지막에 세미콜론(;) 필수

private / protected / public
protected : 자기 자식들한테만.

C++ default는 private.

polymorphism

static resolution, static linkage, early binding : C++와 같은 정적 언어에서는

virtual int area() {} : 컴파일러가 앞에 virtual이 있으면 바인딩하지 않음.
dynamic linkage, late binding

// pure virtual function : 없으면 부모로 올라가지 않고 에러던져버림.
virtual int area() = 0;
