## 191022 객체 Lab06

## **오버로딩**

overload resolution : 자료형에 맞는 해당 메소드랑 연결해주는 것

function overloading이 의미가 있으려면 같은 main scope 안에서 complete가 나야함.

객체 클래스 안에서의 오버로딩

overload + operator to add two Box objects.

Box operator+(const Box& b) {} // const 없어도 됨

this->length + b.length
// box1 + box2 일 때 this는 box1, b는 box2

```
int call by value
int\* pointer
int& call by reference // 대부분의 언어 default
(int& obj2) 를 호출할 때 (obj) 사용 -> 즉, obj를 가리키는 또다른 int형 변수 obj2
```

<table>
<tr>
    <td></td>
    <td>Overloading</td>
    <td>Overriding</td>
</tr>

<tr>
    <td>Name Scope</td>
    <td>같은 scope</td>
    <td>상관 없음</td>
</tr>
</table>
