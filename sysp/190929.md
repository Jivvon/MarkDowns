## 190929 시프

레지스터 중 32비트 = e로 시작

ax = 2바이트 16

esp reighster = pointer register (stack pointer)

ebp register = base pointer

나머지 범용레지스터

데이터 이동명령 : movq Source Dest (명령어 source -> destination)

movq = q(quard) 8바이트 1바이트 movb 2바이트 movd 4바이트 => mova

immediate => 상수 정수 데이터 \$로 시작

register => %로 시작

memory => 괄호로 표시 주소를 포함한 레지스터!

movq 오퍼랜드 조합

메모리 = 포인터

imm - movq $0x4, %rax => temp = 0x4
       - movq $-147,(%rax) => \*p = -147

reg - movq %rax , %rdx = > temp1 = temp2; - movq %rax , (%rdx) => \*p = temp;

Mem -movq (%rax), %rdx => temp = \*p;

movq 8(%rbp) , %rdx => 주소 + 8

D(Rb,Ri,S)
Rb = > 아무레지스터나 가능
Ri => rsp만제외하고 다됨

leaq Src, Dst
